apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Note: Flux Operator uses its own namespace 'operator-system'
# This kustomization applies the SCC and related RBAC

resources:
  - scc.yaml
